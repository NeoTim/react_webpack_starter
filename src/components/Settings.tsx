import React, { Component } from "react";

class Settings extends Component<any, {}> {
    render(): JSX.Element {
        return (
            <div>
                <h1>设置</h1>
            </div>
        );
    }
}

export default Settings;
